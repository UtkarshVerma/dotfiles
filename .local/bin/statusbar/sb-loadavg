#!/bin/sh

case $BLOCK_BUTTON in
	6) "$TERMINAL" -e "$EDITOR" "$0" ;;
esac

icon=" "
threshold=5
load=$(cut -d ' ' -f1 /proc/loadavg)

. sb-theme
if [ 1 -eq $(echo "$load > $threshold" | bc) ]; then
	color=1
fi
display "$icon$load" $color
