#!/bin/sh

j=2
color="$(echo $1 | tr '[:lower:]' '[:upper:]')"
while [ $j -lt ${#1} ]; do
    hex=$(echo $color | cut -c $j-$((j+1)))
    echo "ibase=16;scale=2; $hex/FF" | bc | tr -d '\n'
    [ $j -ne $(( ${#1} - 1 )) ] && printf ","
    j=$((j+2))
done
