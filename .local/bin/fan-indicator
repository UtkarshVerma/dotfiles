#!/bin/sh

notify() {
	notify-send -i sensors-fan-symbolic -h string:x-canonical-private-synchronous:fan $@
}

case $(cat /sys/devices/platform/asus-nb-wmi/fan_boost_mode) in
	0) notify "Silent" ;;
	1) notify "Performance" ;;
	2) notify "Turbo" ;;
esac
